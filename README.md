# 📌 CUSTOM HOOKS HOMEWORK. Задание №3. `useHover`

## 🚀 О проекте:

- Проект был создан с помощью `Create React App`
- Расположение компонентов в папке `components/`
- Расположение хуков в папке `hooks/`
- Вспомогательные функции расположены в папке `core/helpers/`

## ✅ О выполнении задания:

Для реализации был создан хук `useHover`, который:
- Использует `state` для хранения состояния наведения (hover)
- Добавляет обработчики событий `mouseenter` и `mouseleave` через `useEffect`
- Автоматически удаляет обработчики при размонтировании компонента

### 🔄 Возвращаемое значение:
Хук возвращает кортеж в формате:
```javascript
[ref, hovered]
