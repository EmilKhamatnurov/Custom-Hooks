# 📌 CUSTOM HOOKS HOMEWORK. Задание №2. `useLocalStorage`

## 🚀 О проекте:

- Проект был создан с помощью `Create React App`
- Расположение компонентов в папке `components/`
- Расположение хуков в папке `hooks/`  
- Вспомогательные функции расположены в папке `core/helpers/`

## ✅ О выполнении задания:

Для реализации был создан хук `useLocalStorage`, который:
- Использует `state` для хранения данных из LocalStorage
- Предоставляет функции `getValue` и `setValue` для работы с хранилищем
- Применяет `useEffect` для обновления состояния при изменении ключа (`key`)

### 🔄 Возвращаемое значение:
Хук возвращает кортеж в формате:
```javascript
[value, { setItem, removeItem }]
