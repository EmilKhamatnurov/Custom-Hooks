# 📌 CUSTOM HOOKS HOMEWORK. Задание №1. `useFetch`

## 🚀 О проекте:

- Проект создан с использованием `Create React App`
- Компоненты расположены в папке `components/`
- Кастомные хуки находятся в папке `hooks/`
- Вспомогательные функции размещены в `core/helpers/`

## ✅ О выполнении задания:

Реализован хук `useFetch` со следующими особенностями:

### 🔧 Основная функциональность:
- Использует `useState` для хранения данных, состояния загрузки и ошибок
- Применяет `useRef` для отслеживания количества запросов (refetch)
- Выполняет первичный запрос автоматически через `useEffect`
